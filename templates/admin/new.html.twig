{% extends 'base.html.twig' %} {% block title %}Espace Admin - Création
utilisateur{% endblock %} {% block body %}
<main class="pt-5">
    <section id="admin-account" class="container-fluid py-5">
        <div class="container">
            <h1 class="text-center mb-5">Création d'un utilisateur</h1>

            <div class="mx-auto form-add">
                {{ include("admin/_form.html.twig") }}
            </div>

            <div class="text-center mt-5">
                <a class="btn" href="{{ path('admin_dashboard') }}"
                    >Revenir au tableau de bord</a
                >
            </div>
        </div>
    </section>
</main>
{% endblock %}
